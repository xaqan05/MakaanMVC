@model List<Designation>
<div class="container">
    <div class="card-header py-3 d-flex justify-content-between align-items-center">
        <h6 class="m-0 font-weight-bold text-primary">Designation List</h6>
        <a asp-action="Create" asp-controller="Designation" class="btn btn-outline-primary">Create</a>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Created Time</th>
                <th colspan="3" scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td>@item.Name</td>
                    <td>@item.CreatedTime</td>
                    <td>
                        <a asp-controller="Designation" asp-action="Update" asp-route-id="@item.Id" class="btn btn-warning">Update</a>
                        <a asp-controller="Designation" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                        @if (item.IsDeleted)
                        {

                            <a asp-controller="Designation" asp-action="Show" asp-route-id="@item.Id" class="btn btn-success">Show</a>
                        }
                        else
                        {
                            <a asp-controller="Designation" asp-action="Hide" asp-route-id="@item.Id" class="btn btn-secondary">Hide</a>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
